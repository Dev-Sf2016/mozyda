{% extends 'base.html.twig' %}
{% block body %}


    <section id="vendor-list" class="container" >

        <div class="row">

            <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                <div class="panel panel-default">
                    <div class="panel-heading">Manager Customers</div>
                    <div class="panel-body">

                        <!--start of panel body -->
                        <table class="table table-striped table-responsive">
                            <thead>
                            <tr>
                                <th><i class="fa fa-newspaper-o"></i> {{ 'Customer Email'|trans }}</th>
                                <th><i class="fa fa-calendar"></i> {{ 'Creation Date'|trans }}</th>
                                <th><i class="fa fa-calendar"></i> {{ 'Name'|trans }}</th>
                                <th><i class="fa fa-cog"></i> {{ 'Status'|trans }}</th>
                                <th><i class="fa fa-cogs"></i>  {{ 'Actions'|trans }} </th>
                            </tr>
                            </thead>
                            <tbody>



                            {% for customer in customers %}
                                <tr>
                                    <td>{{ customer.email }}</td>
                                    <td>{{ customer.created|date('Y-m-d') }}</td>
                                    <td>{{ customer.name }}</td>
                                    <td>
                                        {% if customer.isActive == 1 %}
                                            <span class="label label-success"><i class="fa fa-check"></i> {{ 'Active'|trans }}</span>
                                        {% else %}

                                            <span class="label label-danger"><i class="fa fa-close"></i> {{ 'In Active'|trans }}</span>
                                        {% endif %}
                                    </td>
                                    <td>

                                        {#<a href="{{ path('admin_vendor_edit', { id: vendor.id }) }}" class="btn btn-sm btn-primary">#}
                                        <a href="#" class="btn btn-sm btn-primary">
                                            <i class="fa fa-edit"></i> {{ 'Edit'|trans }}
                                        </a>
                                    </td>

                                </tr>


                            {% else %}
                                <td colspan="4" align="center">{{ 'No record found'|trans }}</td>
                            {% endfor %}
                            </tbody>
                        </table>
                        <div class="navigation text-center">
                            {{ pagerfanta(customers, 'twitter_bootstrap3', { routeName: 'admin_customers_paginated' }) }}
                        </div>
                        <!--end of  of panel body -->
                    </div>
                </div>
            </div>


        </div>
        </div>
    </section>
    {% endblock %}
