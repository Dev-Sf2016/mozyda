{% extends 'base.html.twig' %}
{% block topmenu %}
    {{ dump(app.user) }}
<a href="{{ path('admin_logout') }}"> Logout </a>
    {% endblock %}
{% block body %}
    <div id="wrapper">
        <div id="container">
            <div id="welcome">
                <h1><span>Welcome to</span> Admin Panel</h1>
                <h2><a href="{{ url('admin_customers') }}">{{ 'Manage Customers'|trans }}</a> </h2>
                <h2><a href="{{ url('admin_companies') }}">{{ 'Manage Companies'|trans }}</a> </h2>
                <h2>( <a href="{{ url('admin_logout') }}" >{{ 'Logout'|trans }}</a>) </h2>
            </div>




        </div>
    </div>
{% endblock %}

{% block stylesheets %}

{% endblock %}
