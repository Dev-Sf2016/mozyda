{% extends '::baseV2.html.twig' %}

{% block body %}

    <div class="row heading-discount">
        <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 heading-stores">
            {{ 'New Discounts'|trans }}
        </div>
    </div>
    <div class="row row_discounts">
        {% for discount in discounts %}
            <div class="col-lg-3 col-md-3 col-sm-6 col-xs-6 padding5">
                <div class="thumbnail_container">
                    <div class="thumbnail">
                        <img src="{{ asset('files/company/discounts/' ~ discount.company.id ~ '/' ~ discount.promotion) }}"/>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

    <div class="row">
                <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12 heading-stores">
                        {{ 'New Stores'|trans }}
                </div>
            </div>
            <div class="row">
                {% for company in companies %}
                    <div class="col-lg-2 col-md-2 col-sm-6 col-xs-6 padding5">
                        <div class="thumbnail_container">
                            <div class="thumbnail">
                                <img src="{{ asset('files/company/logo/' ~ company.logo) }}"/>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
{% endblock %}

{% block stylesheets %}

    {#<a href="{{ path('customer_registration') }}">#}
    {#<i class="fa fa-home"></i> {{ 'Register As customer'|trans }}#}
    {#</a>#}
{% endblock %}
